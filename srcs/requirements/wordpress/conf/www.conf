[www]
user = nobody
group = nobody

# Tímto říkáme PHP, ať naslouchá na portu 9000
listen = 9000

# Nastavení procesů
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3